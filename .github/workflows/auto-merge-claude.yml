# TEMPORALMENTE DESACTIVADO
# name: Auto-merge Claude PRs
#
# on:
#   pull_request_target:
#     types: [opened, synchronize]
#     branches:
#       - claude_bot
#
# jobs:
#   auto-approve-and-merge:
#     # Auto-merge para CUALQUIER PR hacia claude_bot
#     runs-on: ubuntu-latest
#
#     permissions:
#       pull-requests: write
#       contents: write
#
#     steps:
#       - name: Merge PR directly (skip approval)
#         run: |
#           gh pr merge --squash "${{ github.event.pull_request.number }}"
#         env:
#           GH_TOKEN: ${{ secrets.PERSONAL_GITHUB_ACCESS_TOKEN }}
#           GH_REPO: ${{ github.repository }}
